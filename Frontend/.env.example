# Local dev: leave VITE_API_BASE_URL unset — Vite proxies /api → http://localhost:4000
# VITE_API_BASE_URL=

# Production (Railway frontend rebuild — baked in at build time):
VITE_API_BASE_URL=https://meshboard-super-node.up.railway.app/api

VITE_SUPERNODE_ID=SUPERNODE-ARUSHA-01
