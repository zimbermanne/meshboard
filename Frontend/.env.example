# Local dev: leave VITE_API_BASE_URL unset — Vite proxies /api → http://localhost:4000
# VITE_API_BASE_URL=

# Production build (Railway): set to backend URL ending in /api (NOT /api/nodes)
# VITE_API_BASE_URL=https://your-backend.up.railway.app/api

VITE_SUPERNODE_ID=SUPERNODE-ARUSHA-01
