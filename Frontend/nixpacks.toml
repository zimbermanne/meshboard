# Node-only — avoid Caddy static provider; dist rebuilt at start (see railway.toml)
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run build && node serve.js"
